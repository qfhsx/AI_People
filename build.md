# AI数字人教育视频生成系统 - 可行性验证版需求文档
## 文档信息
| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.2（可行性验证版） |
| 编写日期 | 2025-12-25 |
| 适用架构 | Vue3（前端） + Python3.9.6/Flask（后端） |
| 核心依赖 | 火山引擎克隆数字人API（文档链接：|
| 核心目标 | 简化流程，验证“输入题目→后端调用火山引擎API→生成数字人+黑板视频”全链路可行性，无需数据库/复杂存储 |

## 一、项目概述
### 1.1 核心目标
聚焦最小可行流程，验证前后端通信、火山引擎克隆数字人API调用、视频合成全链路通顺，不追求复杂功能和数据持久化，仅保留核心流程验证能力。

### 1.2 技术栈要求（简化版）
| 维度 | 技术选型 | 版本/要求 |
| --- | --- | --- |
| 前端 | 框架/构建工具 | Vue3 + Vite |
|  | UI组件库 | Element Plus（仅用基础组件，简化开发） |
|  | 网络请求 | Axios（仅实现核心接口调用） |
| 后端 | 语言/框架 | Python 3.9.6 + Flask 2.0+ |
|  | 核心依赖 | 火山引擎SDK、FFmpeg、TTS工具（简化版） |
|  | 存储 | 仅用本地临时文件存储（无数据库） |
| 部署 | 启动脚本 | Shell（Linux/Mac） + Batch（Windows） |

## 二、功能需求（极简核心流程）
### 2.1 前端功能模块（仅保留核心）
| 模块 | 子功能 | 详细需求 |
| --- | --- | --- |
| 首页/生成页面 | 内容输入 | 1. 简单文本框输入题目/知识点（限制1000字内，适配免费试用200秒视频额度）<br>2. 无文件上传功能（简化流程） |
|  | 参数配置 | 1. 数字人固定使用火山引擎测试数字人（250623-zhibo-linyunzhi，无需选择）<br>2. 语速固定为1倍速（简化参数）<br>3. 黑板风格固定为简约风（无需选择）<br>4. 分辨率固定为720P（简化输出） |
|  | 提交与进度 | 1. 文本非空校验，提交生成请求<br>2. 显示加载状态和生成进度（0-100%）<br>3. 成功后展示视频播放/下载按钮，失败提示错误信息 |
| 通用功能 | 消息提示 | 仅保留基础Toast提示（提交成功/失败/API调用异常） |

### 2.2 后端功能模块（仅保留核心流程）
| 模块 | 子功能 | 详细需求 |
| --- | --- | --- |
| 接口层 | 核心接口（仅2个） | 1. 视频生成接口（POST）：接收前端输入文本，返回临时任务ID（无需持久化）<br>2. 进度/结果查询接口（GET）：根据任务ID返回进度或视频临时URL |
|  | 接口规范 | 统一JSON响应格式：<br>{<br>  "code": 200/500/400,<br>  "msg": "成功/失败原因",<br>  "data": { "taskId": "xxx", "progress": 50, "videoUrl": "xxx" }<br>} |
| 核心服务 | 文本处理 | 1. 接收前端文本，简单分段（无需复杂解析）<br>2. 校验文本长度，确保生成音频时长≤200秒（免费试用限额） |
|  | TTS转换 | 调用简化版TTS工具（如pyttsx3）将文本转为音频文件（格式符合火山引擎要求） |
|  | 火山引擎API对接 | 1. 固定使用测试数字人resource_id（250623-zhibo-linyunzhi）<br>2. 上传音频文件获取audio_url，调用火山引擎数字人视频生成接口<br>3. 忽略克隆数字人创建流程（仅用测试账号）<br>4. 遵循1并发限制，无需任务队列（同一时间仅处理1个请求） |
| 视频合成 | 黑板与融合 | 1. 生成固定简约风黑板视频（仅展示文本分段内容，同步音频节奏）<br>2. 使用FFmpeg将火山引擎返回的数字人视频与黑板视频合成最终MP4<br>3. 合成后视频存储在临时目录（./backend/temp，无需长期保存） |
| 进度管理 | 进度跟踪 | 1. 手动标记进度节点：文本处理（20%）→ TTS转换（40%）→ API调用（60%）→ 视频融合（90%）→ 完成（100%）<br>2. 前端通过轮询查询进度（简化WebSocket） |
| 异常处理 | 基础异常捕获 | 仅捕获核心异常：火山引擎API调用失败（权限/并发/额度耗尽）、FFmpeg执行失败、TTS转换失败，返回明确错误信息 |
| 存储 | 临时存储 | 1. 音频文件、合成视频均存储在./backend/temp目录<br>2. 无需持久化任何数据，服务重启后临时文件可删除（不影响验证） |

## 三、非功能需求（极简适配）
1. 性能：单次生成流程（从提交到完成）≤5分钟（满足验证需求即可）
2. 兼容性：仅适配Chrome浏览器，后端仅需支持Windows/Linux（开发者常用系统）
3. 安全：无需鉴权、无需加密，仅做基础文本XSS过滤（避免接口异常）
4. 可维护性：核心代码添加简单注释，无需复杂日志（仅控制台打印关键信息）

## 四、工程结构与启动脚本（简化版）
### 4.1 工程目录结构（最小化）
```
./  # 根目录
├── frontend/  # 前端工程（极简）
│   ├── public/
│   ├── src/
│   │   ├── api/  # 仅封装2个接口
│   │   ├── App.vue  # 单页面实现所有功能
│   │   └── main.js
│   ├── package.json
│   └── vite.config.js
├── backend/  # 后端工程（极简）
│   ├── app/
│   │   ├── api/  # 仅2个接口路由
│   │   ├── service/  # 核心服务（文本/TTS/API/融合）
│   │   ├── config.py  # 仅配置火山引擎API密钥（硬编码简化）
│   │   └── app.py  # 后端入口
│   ├── temp/  # 临时存储音频/视频
│   ├── requirements.txt  # 仅列核心依赖
│   └── run.py  # 启动入口
├── start.sh  # Linux/Mac启动脚本
├── start.bat  # Windows启动脚本
└── README.md  # 简单说明（环境要求+启动步骤）
```

### 4.2 启动脚本要求（简化版）
#### 核心目标：一键启动前后端，无需额外配置
- Linux/Mac（start.sh）：
  1. 检查Python 3.9.6和Node.js是否安装（未安装则提示）
  2. 后端：进入backend目录，安装requirements.txt依赖，启动Flask服务（端口5000）
  3. 前端：进入frontend目录，安装npm依赖，启动Vite服务（端口3000）
  4. 输出访问地址：前端http://localhost:3000，后端http://localhost:5000
- Windows（start.bat）：
  1. 检查Python 3.9.6和Node.js环境变量（未配置则提示）
  2. 自动安装后端依赖、启动后端服务
  3. 自动安装前端依赖、启动前端服务
  4. 提示服务启动成功，按任意键关闭

## 五、火山引擎API接入简化要求
1. 无需开通正式服务，仅使用免费试用权限
2. 后端配置文件硬编码火山引擎AccessKey/SecretKey（简化配置，仅用于验证）
3. 固定使用测试数字人resource_id和200秒以内视频生成额度
4. 忽略资源包、计费监控等复杂逻辑，仅需调用成功即可

## 六、交付物清单（极简）
1. 前后端工程代码（符合简化目录结构，仅含核心逻辑）
2. 一键启动脚本（start.sh/start.bat）
3. 依赖配置文件（package.json、requirements.txt）
4. 简化版README.md（仅含环境要求、启动步骤、核心流程说明）

## 七、验收标准（仅验证可行性）
1. 流程通顺：前端输入文本→提交请求→后端调用火山引擎API→生成融合视频→前端播放/下载，全链路无阻断
2. 功能可用：生成的视频包含测试数字人讲解和简约黑板文本，可正常播放
3. 启动便捷：一键启动脚本可成功启动前后端，无需额外手动配置
4. 异常反馈：基础异常（文本为空/API密钥错误/额度耗尽）可正常提示